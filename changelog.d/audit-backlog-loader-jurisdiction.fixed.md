Corpus loader and discovery hardening (#19, #21, #23, #24): target collisions and nonexistent roots raise instead of silently degrading the corpus; dotdirs, underscore dirs, `programs/`, and jurisdiction-patterned subdirectories are never ingested; the discovery fallback prefix derives from the program's own country family instead of a hardcoded `us`; producers rank by jurisdiction specificity so `[us, us-ny]` cannot shadow the state override.
