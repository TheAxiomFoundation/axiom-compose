Silent-pass gaps closed (#20, #22, #25, #26): declared outputs must be produced by the composed module; emitted imports must resolve in the loaded corpus; auto-gating a corpus-produced output no longer exempts it from the eligibility-coverage assertion; specs reject unknown keys and round-trip auto_gate_outputs; transformations reject empty conjunctions, boolean when_false, and non-grammar identifiers; same-named rules across imported modules are a loud ambiguity error (data relations exempt); coverage polices Judgment rollups only.
