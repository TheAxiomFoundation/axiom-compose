Pre-merge adversarial review fixes: producer ranking works within the program's jurisdiction family (a foreign state prefix can no longer outrank the program's own country); Bool/Boolean dtype aliases count as judgment gates for coverage and auto-gate via one shared predicate; acknowledged or mutually-referencing sibling outputs no longer un-police a rollup; module-less corpus states keep the documented no-roots CLI behavior; legacy repos' jurisdiction-lookalike content dirs (`ui/`, `id/`) and root-level modules still load; the auto-gate dtype refusal is spec-deterministic; the CLI surfaces ComposeErrors as clean diagnostics; spec known-keys derive from the dataclass fields.
